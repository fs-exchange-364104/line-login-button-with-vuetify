import LineLoginButton from '@/components/LineLoginButton'

LineLoginButton.install = function install (Vue) {
  Vue.component(LineLoginButton.name, LineLoginButton)
}

export default LineLoginButton
